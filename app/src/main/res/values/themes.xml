<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.OdysseeDesVagabonds" parent="Theme.MaterialComponents.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorAccent</item>
        <item name="colorPrimaryVariant">@color/colorAccent</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/colorPrimary</item>
        <item name="colorSecondaryVariant">@color/colorPrimary</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->

    </style>

    <!-- Custom style for menu background -->
    <style name="CustomPopupMenu" parent="Widget.AppCompat.PopupMenu.Overflow">
        <item name="android:windowBackground">@color/colorPrimary</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:navigationBarColor">@color/colorPrimary</item>
    </style>


    <style name="Theme.OdysseeDesVagabonds.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:actionMenuTextAppearance">@color/colorPrimary</item>
    </style>

    <style name="Theme.OdysseeDesVagabonds.fab" parent="ThemeOverlay.Material3.FloatingActionButton.Primary">
        <item name="backgroundTint">@color/colorAccent</item>
    </style>

    <style name="Theme.OdysseeDesVagabonds.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.OdysseeDesVagabonds.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.OdysseeDesVagabonds.title">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/title_h1</item>
    </style>

    <style name="Theme.OdysseeDesVagabonds.text">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/normal_text</item>
    </style>

    <style name="CustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorHeight">1dp</item>
    </style>
</resources>